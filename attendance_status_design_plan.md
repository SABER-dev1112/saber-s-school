# خطة تنفيذ تصميم القائمة المنسدلة المخصصة لحالة المعلم (Custom Modern Select)

تحويل القائمة المنسدلة الافتراضية للمتصفح في شاشة التحضير إلى مكوّن مخصص فاخر (Custom UI Component) يتبع معايير UI/UX Pro، مع تمييز لوني تفاعلي فوري لحالتي (حاضر / غائب) ودعم العرض المكتبي والجوال.

---

## 1. الهيكلية المعمارية والمكونات المقترحة

### [جديد] [src/components/common/AttendanceStatusSelect.js](file:///d:/downloads/استاذ%20صابر/مواقع/saber%20school%20managment/src/components/common/AttendanceStatusSelect.js)
- مكوّن React نقي ومستقل ومسؤول حصرياً عن واجهة اختيار الحالة (حاضر / غائب).
- **الخصائص والوظائف:**
  - عرض الحالة الحالية مع أيقونة SVG متناسقة (أيقونة تحقق خضراء للحاضر، وأيقونة تنبيه/إغلاق حمراء للغائب).
  - سهم مخصص SVG يدور بنعومة 180 درجة عند فتح القائمة (`transform: rotate(180deg)`).
  - تغيير خلفية وحدود الحقل ديناميكياً:
    - **حاضر (`present`):** خلفية خضراء ناعمة (`#F0FDF4`)، إطار زمردي (`#86EFAC`)، نص أخضر داكن (`#15803D`).
    - **غائب (`absent`):** خلفية حمراء ناعمة (`#FEF2F2`)، إطار وردي (`#FCA5A5`)، نص أحمر بارز (`#B91C1C`).
  - قائمة خيارات عائمة (Floating Dropdown) بظلال ناعمة (Soft Elevation)، مع إغلاق تلقائي عند النقر خارج العنصر (Click-Outside Handler) أو الضغط على زر `Escape`.
  - معالجة الـ `disabled` أثناء إرسال التحضير أو قفل السجل لمنع أي تلاعب.

### [تعديل] [src/app/employee/page.js](file:///d:/downloads/استاذ%20صابر/مواقع/saber%20school%20managment/src/app/employee/page.js)
- استيراد واستخدام `AttendanceStatusSelect` بدلاً من الـ `<select>` الافتراضي في:
  1. جدول التحضير المكتبي (Desktop Table).
  2. بطاقات العرض على الجوال (Mobile Cards).
- ربطه مباشرة بنفس دالة `handleStatusChange(teacher.id, status)` دون المساس بمنطق حفظ البيانات أو الـ State.

---

## 2. خطة التحقق والاختبار (Verification Plan)

### الاختبار الآلي (Automated Tests):
- تشغيل `npm run build` للتأكد من خلو الشيفرة من أي أخطاء ترجمة (Build & TypeScript errors).

### التحقق اليدوي (Manual Verification):
- فتح شاشة التحضير والتأكد من المظهر العصري للقائمة.
- اختبار تبديل الحالة من "حاضر" إلى "غائب" والتأكد الفوري من:
  - تحول لون الحقل إلى اللون الأحمر الناعم مع الأيقونة المناسبة.
  - إخفاء أزرار التوقيت والتأخير بسلاسة.
- اختبار التحويل إلى "حاضر" والتأكد من استعادة اللون الأخضر وأزرار الوقت.
- اختبار العرض على مقاس الشاشات الصغيرة (الجوال) والتأكد من سهولة النقر والتفاعل (Touch-friendly).
- اختبار إغلاق القائمة عند النقر في أي مكان آخر بالصفحة.
