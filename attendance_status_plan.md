# خطة تبسيط قائمة الحالة اليومية للمعلم (حاضر / غائب فقط)

تعديل خيارات القائمة المنسدلة لحالة المعلم اليومية في شاشة التحضير لموظف التسجيل لتقتصر على خيارين فقط هما: **"حاضر"** و **"غائب"**، وإزالة خيارات (غياب بدون إذن) و (إجازة طارئة)، مع الحفاظ التام على بقية الميزات ومسار حفظ البيانات.

---

## 1. التغييرات في الكود

### [تعديل] [src/app/employee/page.js](file:///d:/downloads/استاذ%20صابر/مواقع/saber%20school%20managment/src/app/employee/page.js)
1. **جدول العرض المكتبي (Desktop View):**
   - في خانة "الحالة اليومية" (السطر 698-701)، تعديل خيارات الـ `<select>` لتكون:
     - `<option value="present">حاضر</option>`
     - `<option value="absent">غائب</option>`
   - إزالة `<option value="emergency_pending">إجازة طارئة (انتظار الاعتماد)</option>`.

2. **بطاقات عرض الجوال (Mobile View):**
   - في خانة "الحالة" للهواتف الذكية (السطر 929-932)، تعديل خيارات الـ `<select>` لتكون:
     - `<option value="present">حاضر</option>`
     - `<option value="absent">غائب</option>`
   - إزالة خيار إجازة طارئة.

3. **الحفاظ على سلامة البيانات والمنطق:**
   - الحفاظ على قيم `value="present"` و `value="absent"` حتى لا يتأثر تخزين قاعدة البيانات (Supabase / LocalStorage) أو حسابات الحضور والتأخير أو التقارير.
   - الإبقاء على تفاصيل التأخير (في الموعد / متأخر) ووقت الحضور كما هي للمعلم الحاضر.

---

## 2. خطة التحقق والاختبار (Verification Plan)

### التحقق اليدوي (Manual Verification):
- فتح شاشة تحضير الموظف (`/employee`).
- فتح القائمة المنسدلة لخانة "الحالة اليومية" والتأكد من ظهور خيارين فقط:
  1. حاضر
  2. غائب
- التأكد من تجربة العرض المكتبي وعرض الجوال.
- التأكد من أن اختيار "حاضر" يُظهر أزرار التوقيت والتأخير بشكل طبيعي، واختيار "غائب" يُلغي متطلبات الوقت.
- حفظ الحضور والتحقق من حفظ السجل دون أي أخطاء في الكونسول أو في التخزين.
